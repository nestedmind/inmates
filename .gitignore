# Identity tokens. Never commit these.
gh-*-token
gh-*-token.*
!gh-*-token.example
*.token
.env
.env.*
!.env.example

# Coordinator state (Larceny/Scofield). Local to this machine; commit if the
# team wants to share it.
.larceny/
