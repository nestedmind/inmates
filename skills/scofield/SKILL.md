---
name: scofield
description: Moved. Use when coordinating several subagent coders against tickets the owner has signed off on, including dispatching tickets, reviewing what comes back, merging, and escalating. Also use when the user names Scofield.
---

# Scofield (moved)

This skill moved. Follow the `coordinator` skill instead — it holds the full content that used to live here.

This stub exists only so that a project-level override file written before this rename, whose body says "Follow the `scofield` skill", still resolves correctly. If you are writing a new override file, point it at the `coordinator` skill instead.
